/******************************************************************************
 *  render.h  -  3D渲染模块声明
 *
 *  负责绘制3D场景（地面、网格、坐标轴）和鼠标3D拾取。
 ******************************************************************************/
#ifndef RENDER_H
#define RENDER_H

#include "common.h"     // 需要 Camera3D 等类型

/* 绘制整个3D场景：地面网格、坐标轴、所有无人机 */
void Draw3D(void);

/* 鼠标3D拾取：检测用户点击了哪架无人机，返回索引（-1=没点到） */
int Pick(void);

#endif  /* RENDER_H */
