# 无人机编队灯光秀模拟器 — 设计文档

---

## 一、前言

### 1.1 编写背景

近年来，无人机编队表演作为一种新兴的艺术表现形式，在国内外大型活动中频频亮相——从春晚夜空中的璀璨光影，到奥运会开幕式上震撼人心的空中编队，无人机灯光秀正在重新定义人类对天空的想象。然而，一场真实的无人机编队表演背后，需要耗费巨大的人力、物力和时间成本：每一架无人机的飞行路径需要经过精密的计算，每一个灯光效果都需要反复的调试，任何一个环节的疏漏都可能导致整场表演的失败。

在学习 C 语言程序设计的过程中，我深刻体会到，理论知识与实际应用之间往往存在一道难以逾越的鸿沟。课堂上学习的数据结构、算法和图形学知识，需要一个具体的、可视化的项目来将它们串联起来。无人机编队模拟恰好提供了这样一个绝佳的场景——它既包含了三维空间中的坐标计算与轨迹插值，又涉及了用户交互界面的设计与实时渲染，同时还考验着程序架构的合理性与代码的可维护性。

正是基于这样的背景，我决定开发一款轻量级的无人机编队灯光秀模拟器。不同于市面上动辄上万元的专业仿真软件，本软件以简洁实用为核心设计理念，力求用最少的代码行数实现最核心的功能，让使用者能够在几分钟内上手，快速构建属于自己的无人机灯光秀。

### 1.2 编写目的

本软件的首要目的是为无人机编队表演提供一个**快速原型验证工具**。在实际的编队表演策划中，导演和编舞师往往需要在正式编程之前快速预览队形变换和灯光效果。传统的专业软件操作复杂、学习曲线陡峭，而本软件通过三步式工作流（Setup → Edit → Show），让用户能够在极短的时间内完成"创建无人机 → 编排轨迹 → 回放演示"的完整流程，大大降低了原型验证的时间成本。

其次，本软件也是 C 语言与计算机图形学教学的一个**实践案例**。通过阅读和修改本项目约三百行的源代码，学习者可以直观地理解三维坐标变换、线性插值、射线碰撞检测、实时渲染循环等核心概念。项目所使用的 raylib 图形库因其简洁的 API 设计和丰富的示例程序，特别适合作为从控制台编程迈向图形界面编程的第一座桥梁。

此外，本软件的设计遵循**"界面丰富、功能精简"**的原则——在图形界面上提供充足的按钮和输入控件，给予用户完整的控制感；但在底层功能上只实现最核心的需求，避免代码膨胀。这种设计哲学使得软件既能满足当前的使用需求，又为未来的功能扩展预留了清晰的结构框架。

### 1.3 参考资料

| 书名 | 作者 | 出版社 | 出版年份 |
|------|------|--------|----------|
| 《C 高级应用程序设计》 | 王世元 | 清华大学出版社 | 2005 |
| 《C 语言程序设计教程》 | 周纯杰、何鼎新 等 | 华中科技大学出版社 | 2007 |
| 《C++ Primer Plus（第6版）》 | Stephen Prata | 人民邮电出版社 | 2012 |
| 《数据结构（C 语言版）》 | 严蔚敏、吴伟民 | 清华大学出版社 | 2007 |

### 1.4 参考软件

本项目的开发受到以下软件的启发：

- **SkyBrush 3D Drone Show Designer**：一款专业的无人机编队设计软件，其三步式工作流（设计→仿真→部署）为本项目的模式切换架构提供了设计思路。
- **raylib 官方示例集**：raylib 图形库自带的 3D 渲染示例，尤其是 `core_3d_camera_free.c` 和 `models_billboard.c`，为本项目的 3D 场景搭建提供了直接的技术参考。
- **Blender 3D**：开源三维建模软件中的轨迹编辑器和关键帧动画系统，启发了本项目路径点轨迹编辑功能的设计。

### 1.5 编者的话

本项目从最初的一个简单想法——"能不能用 C 语言做一个看得见的无人机模拟器"——到如今一个具备完整三阶段工作流、实时 3D 渲染和轨迹回放功能的图形化应用程序，中间经历了多次推倒重来和反复打磨。从第一版的 1160 行臃肿代码，到最终精简约 351 行的简洁实现，我深刻体会到了"少即是多"的编程哲学。

在设计过程中，我始终坚持一个原则：**让用户第一眼就知道该怎么操作**。因此，右侧面板始终保留 `1.Setup`、`2.Edit`、`3.Show` 三个醒目的模式切换按钮，每一步都有清晰的文字引导。同时，为了减少用户的认知负担，我刻意将灯光颜色限制为红、绿、蓝三种——足够演示核心概念，又不会让初学者感到困惑。所有的坐标输入采用文字框直接键入的方式，避免了滑块拖拽的不精确性，坐标轴的颜色（X=红、Y=绿、Z=蓝）与输入框标签颜色一一对应，降低了空间定位的学习成本。

由于时间和能力所限，本软件目前仅实现了单机轨迹编辑和简单的线性回放功能。未来的版本中，我希望能加入碰撞检测、多机同步编队变换、轨迹文件导入导出等功能，使其真正具备实际应用的价值。

---

## 二、任务概述

### 2.1 目标

本项目的开发目标是构建一个**轻量级、图形化、实时交互**的无人机编队灯光秀模拟器。具体而言：

1. **可视化**：在 3D 场景中实时呈现无人机的空间位置、灯光状态和运动轨迹，让用户对编队效果一目了然。
2. **可编排**：允许用户为每架无人机独立设置运动路径（路径点序列），实现个性化的轨迹设计。
3. **可回放**：将编排好的轨迹以动画形式完整播放，支持播放/暂停/停止和速度调节。
4. **低门槛**：操作流程直观清晰，无需专业培训即可上手使用。
5. **可扩展**：代码结构清晰、模块化程度高，为后续功能扩展奠定基础。

### 2.2 已实现功能

目前软件已实现以下核心功能：

| 功能模块 | 具体功能 | 实现方式 |
|----------|----------|----------|
| **无人机创建** | 逐个创建无人机，键盘输入三维坐标（X/Y/Z），点击选择红/绿/蓝灯光颜色 | Setup 模式下的文字输入框 + 颜色选择按钮 |
| **3D 实时预览** | 无人机以发光球体形式在 1/4 象限地面上即时显示，支持鼠标滚轮缩放 | raylib 3D 渲染管线，固定斜视角相机 |
| **灯光控制** | 每架无人机支持关闭（OFF）、常亮（ON）、闪烁（BLINK）三种灯光模式 | Edit 模式下的三状态按钮组 |
| **路径点编辑** | 为选中无人机添加运动目标点（X/Y/Z 坐标），路径点以黄色小球和连线在 3D 视图中可视化 | Edit 模式下的文字输入 + 添加按钮 |
| **轨迹回放** | 所有无人机沿各自路径点序列以线性插值方式运动，播放速度 0.5x~8x 可调 | Show 模式下的播放控制 + 速度滑块 |
| **无人机管理** | 支持删除无人机、Tab 键切换选中、F 键聚焦、鼠标点击 3D 拾取 | 键盘快捷键 + 射线碰撞检测 |
| **输入冲突防护** | 文字输入时自动禁用 1/2/3 灯光快捷键和 A 添加键，防止误操作 | txtFocus 全局标志位机制 |
| **状态反馈** | 顶部消息栏实时显示操作结果（创建成功、路径点已添加、回放完成等） | Msg() 消息系统 |

### 2.3 功能组织逻辑

上述功能的组织遵循**三段式线性工作流**：

```
Setup（创建） → Edit（编排） → Show（回放）
```

这种设计借鉴了视频编辑软件中的"导入素材→编辑时间线→导出播放"流程。三个阶段之间通过右侧面板顶部的三个大按钮自由切换，但业务逻辑上有自然的先后顺序——必须先创建无人机，才能编辑轨迹；必须先设置路径点，回放时才有运动动画。

为了在代码层面保持这种组织逻辑的清晰性，程序使用了一个全局枚举变量 `Mode`（取值为 `M_SETUP`、`M_EDIT`、`M_SHOW`）来控制整个 UI 面板的分支渲染。UI 函数 `DrawUI()` 内部通过 `if/else if` 结构根据当前模式绘制不同的控件组，使得每种模式下的界面和逻辑完全独立，互不干扰。这种"状态机 + 分支渲染"的架构，用不到 50 行代码就实现了三种模式的完整切换，避免了引入多个窗口或多个源文件的复杂性。

### 2.4 编写规范

- **命名规范**：全局变量采用单字母大写（`D[]`、`N`、`S`、`M`），函数名采用三字母缩写（`Btn`、`Sld`、`Txt`、`Rst`、`Upd`），以最大化减少代码行数同时保持可读性。
- **缩进风格**：采用紧凑的 K&R 风格，函数体左大括号不换行。
- **注释风格**：每个功能模块以 `/* ========== 模块名 ========== */` 分隔，关键逻辑处配有单行注释。
- **常量定义**：所有数值统一使用 `#define` 宏定义，方便统一调整。

---

## 三、运行环境与配置

### 3.1 硬件环境

| 项目 | 最低配置 | 推荐配置 |
|------|----------|----------|
| CPU | Intel Core i3 或同等性能处理器 | Intel Core i5 及以上 |
| 内存 | 4 GB RAM | 8 GB RAM |
| 显卡 | 支持 OpenGL 3.3 的集成显卡 | 独立显卡，支持 OpenGL 4.0+ |
| 显示器 | 分辨率 1024×768 | 分辨率 1280×720 及以上 |
| 输入设备 | 鼠标 + 键盘 | 带滚轮的鼠标 |

### 3.2 软件环境

| 项目 | 说明 |
|------|------|
| **操作系统** | Windows 10 / Windows 11（64 位） |
| **编译器** | MinGW-w64 GCC 8.1.0（x86_64-win32-seh） |
| **图形库** | raylib 5.5（win64_mingw-w64 预编译版本） |
| **依赖库** | OpenGL 3.3+、GLFW（随 raylib 附带） |
| **开发工具** | Visual Studio Code（简称 VS Code） |
| **构建方式** | 命令行直接编译，无需 Makefile 或 CMake |

### 3.3 软件接口

本程序的软件接口关系如下：

```
┌──────────────────────────────┐
│        drone_light_show.c    │
│         (本项目源代码)        │
├──────────────────────────────┤
│  raylib.h  │  raymath.h      │  ← 图形渲染 + 数学计算
├────────────┴─────────────────┤
│  libraylibdll.a (导入库)     │
│  raylib.dll     (动态链接库) │
├──────────────────────────────┤
│  OpenGL 3.3  │  GLFW         │  ← 底层图形 API
├──────────────┴───────────────┤
│  Windows 11  │  GPU 驱动     │  ← 操作系统 + 硬件
└──────────────────────────────┘
```

- **raylib**：提供窗口管理、3D 渲染（`DrawSphere`、`DrawPlane`、`DrawLine3D` 等）、输入处理（`GetMousePosition`、`IsKeyPressed`、`GetCharPressed`）和数学工具。
- **raymath**：raylib 的数学扩展，提供 `Vector3Distance`、`Vector3Normalize` 等三维向量操作函数。
- **标准 C 库**：`stdio.h`（文件读写、格式化输出）、`stdlib.h`（atof 字符串转浮点数）、`string.h`（字符串操作）、`math.h`（sqrtf 等数学函数）、`stdarg.h`（可变参数宏）。

### 3.4 编译命令

```bash
gcc drone_light_show.c -o drone_light_show.exe -lraylibdll -lopengl32 -lgdi32 -lwinmm
```

链接参数说明：
- `-lraylibdll`：链接 raylib 动态导入库
- `-lopengl32`：Windows OpenGL 支持
- `-lgdi32`：Windows GDI（图形设备接口）
- `-lwinmm`：Windows 多媒体 API（音频支持）

---

## 四、需求分析与系统设计

### 4.1 系统基本框架

本系统采用**单文件、单循环**的经典游戏架构，所有代码位于唯一的源文件 `drone_light_show.c` 中，共 351 行。系统框架的结构如下所示：

```
┌─────────────────────────────────────────────────────┐
│                    main() 主函数                     │
│  ┌───────────────────────────────────────────────┐  │
│  │         初始化 (InitWindow / 相机 / Setup)     │  │
│  └───────────────────┬───────────────────────────┘  │
│                      ▼                               │
│  ┌───────────────────────────────────────────────┐  │
│  │     主循环 while(!WindowShouldClose())         │  │
│  │  ┌──────────┬──────────┬──────────┬────────┐  │  │
│  │  │ Keys()   │ Pick()   │ Update() │ 渲染   │  │  │
│  │  │ 键盘处理 │ 3D拾取   │ 状态更新 │ 3D+UI  │  │  │
│  │  └──────────┴──────────┴──────────┴────────┘  │  │
│  └───────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────┘
```

### 4.2 功能模块树状图

```
main() 程序入口
├── 初始化模块
│   ├── SetConfigFlags()     ← 窗口属性设置（4xMSAA 抗锯齿 + 可缩放）
│   ├── InitWindow()         ← 创建 1280×720 窗口
│   ├── Cam.position/target  ← 固定斜视角相机（远角俯视）
│   └── while 主循环 ──────────────────────────────┐
│                                                   │
├── 输入处理模块                                    │
│   ├── Keys()               ← 键盘快捷键          │
│   │   ├── Tab              ← 切换选中无人机      │
│   │   ├── A (+!txtFocus)   ← 快速创建无人机      │
│   │   ├── Delete           ← 删除选中无人机      │
│   │   ├── 1/2/3 (+!txtFocus)← 灯光 OFF/ON/BLINK  │
│   │   ├── 方向键            ← 微调位置            │
│   │   ├── Space            ← 播放/暂停            │
│   │   ├── F/F1/F2/F3       ← 聚焦/模式切换       │
│   │   └── 鼠标滚轮          ← 缩放视角            │
│   │                                                │
│   └── Pick()               ← 鼠标 3D 拾取        │
│       ├── GetMouseRay()    ← 屏幕坐标→3D射线     │
│       └── GetRayCollisionSphere() ← 射线-球体碰撞 │
│                                                   │
├── 无人机管理模块                                  │
│   ├── MakeDrone()          ← 创建无人机          │
│   │   ├── atof(sx/sy/sz)   ← 坐标输入→浮点数    │
│   │   ├── ic 颜色索引      ← 红/绿/蓝选择        │
│   │   └── N++ (计数递增)   ← 全局计数管理        │
│   │                                                │
│   └── DelDrone()           ← 删除无人机          │
│       └── 数组前移覆盖      ← 紧凑存储            │
│                                                   │
├── 轨迹回放模块                                    │
│   ├── Rst()                ← 重置回放状态        │
│   └── Upd()                ← 逐帧更新位置        │
│       ├── 计算每段距离      ← sqrtf(dx^2+dy^2+dz^2)│
│       ├── 线性插值移动      ← pos += dir/ds*step   │
│       └── 到达路径点        ← ci++ (切换到下一目标)│
│                                                   │
├── 3D 渲染模块                                     │
│   ├── Draw3D()             ← 3D 场景绘制         │
│   │   ├── DrawPlane()      ← 半透明地面(正象限)  │
│   │   ├── 网格线(20×20)    ← 地面参考线          │
│   │   ├── 坐标轴 (X红/Y绿/Z蓝)                   │
│   │   └── DD() ×N          ← 遍历每架无人机      │
│   │                                                │
│   └── DD()                 ← 单架无人机渲染      │
│       ├── DrawSphere()×3   ← 发光球体（核心/光晕）│
│       ├── DrawCircle3D()   ← 选中高亮环          │
│       └── 路径点连线        ← DrawLine3D+DrawSphere│
│                                                   │
├── UI 渲染模块                                     │
│   └── DrawUI()             ← 右侧面板(240px)     │
│       ├── 模式切换三按钮    ← 1.Setup/2.Edit/3.Show│
│       ├── [M_SETUP 分支]                          │
│       │   ├── Txt()×3      ← X(红)/Y(绿)/Z(蓝)  │
│       │   ├── 颜色选择器    ← Red/Green/Blue      │
│       │   ├── Btn("+Create Drone")               │
│       │   └── 已创建列表    ← 点击跳转 Edit       │
│       ├── [M_EDIT 分支]                           │
│       │   ├── Btn OFF/ON/Blink ← 灯光三态按钮    │
│       │   ├── Txt()×3      ← 路径点坐标输入      │
│       │   ├── Btn("+Add Waypoint")               │
│       │   ├── 路径点列表    ← 逐条 + X 删除按钮   │
│       │   └── Btn Delete Drone / <- Setup         │
│       └── [M_SHOW 分支]                           │
│           ├── Sld()         ← 速度滑块            │
│           ├── Btn Play/Pause/Stop                 │
│           └── 进度条        ← DrawRectangle       │
│                                                   │
└── 工具函数模块                                    │
    ├── Btn()                ← 通用按钮(无悬停高亮) │
    ├── Txt()                ← 文字输入框(单激活)   │
    ├── Sld()                ← 滑块控件            │
    ├── Msg()                ← 状态消息            │
    ├── In()                 ← 鼠标区域检测        │
    └── Sep()                ← 分隔线              │
```

### 4.3 数据流图

```
用户输入坐标(sx,sy,sz) → MakeDrone() → D[N].start/pos 写入
     ↓
用户输入路径点(wx,wy,wz) → [Add Waypoint] → D[S].wp[wc++] 写入
     ↓
用户点击 Play → Rst() 重置所有 → Upd() 逐帧更新
     ↓
Upd() 读取 D[i].wp[ci] → 计算插值 → 更新 D[i].pos
     ↓
Draw3D() 读取 D[i].pos → 渲染发光球体在 3D 场景中
```

---

## 五、界面设计与功能介绍

> （此处预留截图位置：请插入程序运行时的完整窗口截图）

### 5.1 整体布局

程序窗口分为三个区域：

| 区域 | 位置 | 内容 |
|------|------|------|
| **3D 视图区** | 左侧主区域 | 实时 3D 渲染场景，仅显示正象限（X≥0, Z≥0），包含地面、网格线、红/绿/蓝坐标轴、无人机球体、路径点标记 |
| **控制面板** | 右侧 240px 宽 | 模式切换按钮 + 当前模式对应的操作控件 |
| **帮助栏** | 左下角 | 半透明背景的快捷键速查表 + 当前模式和无人机数量 |

### 5.2 3D 场景设计

> （此处预留截图位置：3D 场景截图）

3D 场景是本项目的核心视觉输出。与常见的全象限视图不同，本软件采用**1/4 象限设计**——仅显示 X≥0、Z≥0 的正半平面，将原点置于场景远端角落处。这一设计决策基于以下考量：

- **空间直观性**：用户只需输入正数坐标（X、Y、Z 均≥0），无人机便会出现在对应的网格位置上。数字越大，离原点越远，方向感非常清晰。
- **边界可视化**：红线（X 轴）和蓝线（Z 轴）自然形成了地面的两条边界线，绿线（Y 轴）从原点竖直向上延伸，表示高度方向。三条轴线形成了类似于房间角落的空间参考。
- **减轻认知负担**：去掉负半平面的网格线后，场景更加清爽，用户不会因为负坐标的存在而感到困惑。

场景地面为 40×40 单位的半透明灰色平面，上面绘制 20×20 的淡色参考网格。原点处有一个红色小球标记。相机固定在远角高处（约 (55, 48, 70) 位置），以约 30° 的倾斜角俯视整个网格区域，用户可通过鼠标滚轮缩放视角（5~60 单位范围），也可按 F 键聚焦到选中无人机。

### 5.3 Setup 模式 — 无人机创建界面

> （此处预留截图位置：Setup 模式界面截图）

Setup 模式是用户接触的第一个界面。界面上方显示 `[ Setup ] Create Drones` 标题，下方依次排列：

**坐标输入区**：采用三个独立的文字输入框，分别标记为红色的 **X**、绿色的 **Y** 和蓝色的 **Z**，与 3D 场景中的坐标轴颜色一一对应。用户可以直接用键盘输入数字（支持负数和一位小数），Y 轴初始值为 0（贴近地面）。输入框获得焦点时会显示蓝色边框和闪烁光标，失去焦点后恢复灰色——这种清晰的视觉反馈让用户始终知道自己正在操作哪个控件。

**颜色选择区**：提供三个并排的颜色按钮——`Red`（红色）、`Green`（绿色）、`Blue`（蓝色）。当前选中的颜色会显示白色加粗边框。三种颜色已足以满足演示需求。

**创建按钮与列表**：点击绿色的 `+ Create Drone` 按钮后，无人机立即在左侧 3D 视图中生成，同时出现在下方的已创建列表中。列表中的每一项显示编号、名称、坐标和颜色小色块。点击列表中的任意无人机，即可直接跳转到 Edit 模式——这个细节设计显著简化了"创建后立即编辑"的操作路径。

### 5.4 Edit 模式 — 轨迹编排界面

> （此处预留截图位置：Edit 模式界面截图）

Edit 模式是轨迹编排的核心界面。标题显示 `[ Edit ] Light & Trajectory`，其下分为三个功能区块：

**灯光控制区**：三个并排按钮 `OFF`、`ON`、`Blink` 控制选中无人机的灯光状态。当前激活的状态以对应的语义颜色显示——OFF 为灰色（表示熄灭）、ON 为绿色（表示点亮）、Blink 为黄色（表示闪烁）。

**路径点添加区**：与 Setup 模式类似的三个坐标输入框（X=红、Y=绿、Z=蓝），下方是蓝色的 `+ Add Waypoint` 按钮。每点击一次，系统就将当前输入的坐标作为一个路径点追加到选中无人机的轨迹末尾。

**路径点列表**：列表显示已添加的路径点编号和坐标，每条右侧都有一个红色的 `X` 删除按钮。最多显示 6 条（超出显示 `... more ...`）。3D 视图中会将所有无人机的路径点以黄色小球标记，并以黄色虚线连接，形成可视化的"轨迹预览"。

辅助按钮：`Delete Drone`（删除当前无人机）和 `<- Setup`（返回创建界面）。

**输入冲突防护**：当用户在文字输入框中键入坐标时，程序通过 `txtFocus` 全局标志自动禁用 `1`/`2`/`3` 灯光快捷键和 `A` 添加快捷键，防止输入数字时意外改变灯光状态或创建多余无人机。

### 5.5 Show 模式 — 回放界面

> （此处预留截图位置：Show 模式界面截图）

Show 模式提供轨迹动画的完整播放控制。标题显示 `[ Show ] Playback`。

**速度调节**：可拖动的速度滑块（0.5x~8x），是程序中唯一保留的滑块控件（回放速度天然适合连续调节）。

**播放控制**：`Play`（绿）、`Pause`（黄）、`Stop`（红）三个按钮，与通用播放器布局一致。进度条以黄色填充条实时显示回放百分比。

**快捷键**：空格键播放/暂停，Esc 停止。

---

## 六、函数说明

### 6.1 头文件与宏定义

```c
#include "raylib.h"      // 图形渲染核心库：窗口管理、3D绘制、输入处理
#include "raymath.h"     // 数学扩展库：Vector3运算、距离计算
#include <stdio.h>       // 标准I/O：snprintf 格式化字符串
#include <stdlib.h>      // 标准库：atof (字符串→浮点数转换)
#include <string.h>      // 字符串：strlen/strcspn 等
#include <stdarg.h>      // 可变参数：va_list/vsnprintf (消息格式化)
#include <math.h>        // 数学：sqrtf (平方根，距离计算)

#define MAX_DRONES 200   // 最大无人机数量
#define MAX_WP      50   // 每架无人机最多路径点数
#define MAX_NAME    20   // 无人机名称最大长度
#define GROUND      40.0f // 地面/网格尺寸（40×40单位）
#define DR          0.3f  // 无人机球体半径
#define PW          240   // 右侧面板宽度（像素）
```

### 6.2 核心数据结构

```c
typedef enum { L_OFF=0, L_ON, L_BLINK } Light;     // 灯光三态枚举
typedef enum { M_SETUP=0, M_EDIT, M_SHOW } Mode;    // 应用模式枚举
typedef struct { float x,y,z; } Pt;                  // 三维点（替代Vector3）
typedef struct { Pt p; } Waypoint;                   // 路径点

typedef struct {
    char name[MAX_NAME]; Pt start, pos; float h;    // 名称、起始位置、当前位置、高度
    Waypoint wp[MAX_WP]; int wc;                     // 路径点数组及计数
    Light light; int color; float bt; bool bon;      // 灯光模式、颜色、闪烁计时/状态
    int ci; bool fin, act, sel;                      // 回放索引、完成、激活、选中
} Drone;
```

`Drone` 结构体是整个程序的数据核心。将 `Pt` 定义为独立的三维点结构体（而非直接使用 raylib 的 `Vector3`），是为了在后续扩展中保持数据层的独立性。

### 6.3 无人机创建函数

```c
static void MakeDrone(void){
    if(N>=MAX_DRONES){Msg("Max %d drones!",MAX_DRONES);return;}
    float px=(float)atof(sx),py=(float)atof(sy),pz=(float)atof(sz);
    if(py<0.5f)py=0.5f; if(py>30)py=30;   // Y高度限制
    Drone*d=&D[N];memset(d,0,sizeof(Drone));d->act=1;d->light=L_ON;
    d->color=ic;d->bon=1;d->h=py;
    d->start=(Pt){px,py,pz};d->pos=d->start;
    snprintf(d->name,MAX_NAME,"D-%d",N+1);N++;S=N-1;
    Msg("Created %s (%.0f,%.0f,%.0f) [%s]",d->name,px,py,pz,LCN[ic]);}
```

从全局输入缓冲区 `sx/sy/sz` 读取用户输入的坐标字符串，通过 `atof()` 转换为浮点数。Y 坐标限制在 0.5m~30m。新无人机追加到数组末尾并自动选中，操作结果通过 `Msg()` 反馈。

### 6.4 轨迹回放函数

```c
static void Upd(float dt){
    if(!play||pause)return;                    // 未播放或暂停时直接返回
    int done=0;
    for(int i=0;i<N;i++){Drone*d=&D[i];        // 遍历所有无人机
        if(!d->act||d->fin){done++;continue;}   // 跳过无效或已完成的
        if(d->ci>=d->wc){d->fin=1;done++;continue;} // 路径点全部走完
        Pt t=d->wp[d->ci].p;                    // 当前目标路径点
        float dx=t.x-d->pos.x,dy=t.y-d->pos.y,dz=t.z-d->pos.z;
        float ds=sqrtf(dx*dx+dy*dy+dz*dz);     // 到目标点的距离
        if(ds<0.15f){d->ci++;if(d->ci>=d->wc)d->fin=1;} // 到达，切换下一路径点
        else{float step=spd*dt*3;if(step>ds)step=ds;   // 移动步长
            d->pos.x+=dx/ds*step;d->pos.y+=dy/ds*step;d->pos.z+=dz/ds*step;}}
    /* 计算总进度百分比 */
    int total=0,cur=0;
    for(int i=0;i<N;i++){if(!D[i].act)continue;
        total+=D[i].wc>0?D[i].wc:1;
        if(D[i].fin)cur+=D[i].wc>0?D[i].wc:1;else cur+=D[i].ci;}
    prog=total>0?(float)cur/total:0;
    if(done>=N){play=0;Msg("Show finished!");}} // 全部完成自动停止
```

回放系统的核心。每帧对每架无人机计算到目标路径点的方向向量和距离，距离小于阈值（0.15 单位）则切换到下一路径点，否则沿方向移动。所有无人机到达终点后自动停止并显示完成消息。

### 6.5 文字输入框与键盘冲突防护

```c
static int txtFocus=0;  // 全局标志：是否有文字输入框处于激活状态

static int Txt(Rectangle r,char*buf,int max,const char*label){
    ...
    static int activeIdx=-1;             // 静态变量：记录当前激活的输入框ID
    int id=(int)((long long)buf&0xFFF); // 用缓冲区地址的低12位作为唯一ID
    if(IsMouseButtonPressed(MOUSE_LEFT_BUTTON)){
        if(In(r))activeIdx=id;          // 点击此框→激活
        else if(activeIdx==id)activeIdx=-1;} // 点击外部→取消激活
    if(activeIdx==id)txtFocus=1;        // 仅激活的框设置焦点标志
    if(activeIdx==id){                  // 仅激活的框处理键盘输入
        ...处理 GetCharPressed / KEY_BACKSPACE / 负号 / 小数点 ...
        ...绘制闪烁光标...}
    return 0;
}
```

`Keys()` 函数在检测到 `txtFocus` 为 1 时，自动跳过 `1/2/3` 灯光键和 `A` 创建键的处理，彻底避免了文字输入与功能快捷键之间的冲突。`Update()` 每帧将 `txtFocus` 清零，`Txt()` 仅在自身激活时将其置 1——这种"帧重置 + 按需置位"的模式确保了标志位的正确性。

### 6.6 主循环

```c
int main(void){
    SetConfigFlags(FLAG_MSAA_4X_HINT|FLAG_WINDOW_RESIZABLE);
    InitWindow(1280,720,"Drone Light Show");SetTargetFPS(60);
    /* 固定斜视角：从远角高处俯视正象限网格 */
    Cam.position=(Vector3){55,48,70};Cam.target=(Vector3){20,0,15};
    Cam.up=(Vector3){0,1,0};Cam.fovy=50;Cam.projection=CAMERA_PERSPECTIVE;
    while(!WindowShouldClose()){
        float dt=GetFrameTime();
        Keys();                          // 键盘输入处理（含txtFocus冲突检测）
        float wh=GetMouseWheelMove();    // 鼠标滚轮缩放（5~60范围）
        if(wh!=0){...}
        int pk=Pick();                   // 3D射线拾取
        if(pk>=0){if(S>=0)D[S].sel=0;S=pk;D[S].sel=1;if(M==M_SETUP)M=M_EDIT;}
        Update(dt);                      // txtFocus清零 + 灯光闪烁 + 回放更新
        BeginDrawing();ClearBackground(Bg);
        Draw3D();                        // 3D场景（1/4象限地面+无人机+路径点）
        DrawUI();                        // 右侧240px控制面板
        /* 左下角帮助栏：快捷键提示 + 模式/数量 + FPS */
        ...
        EndDrawing();
    }CloseWindow();return 0;}
```

采用标准的"输入→更新→渲染"游戏循环，60 FPS。相机固定斜视角，不随鼠标旋转——因为旋转会导致对无人机运动方向的感知偏差。用户可通过滚轮缩放、F 键聚焦、Tab 键切换选中无人机。
