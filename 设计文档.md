# 无人机编队灯光秀模拟器 — 设计文档

---

## 一、前言

### 1.1 编写背景

近年来，无人机编队表演作为一种新兴的艺术表现形式，在国内外大型活动中频频亮相——从春晚夜空中的璀璨光影，到奥运会开幕式上震撼人心的空中编队，无人机灯光秀正在重新定义人类对天空的想象。然而，一场真实的无人机编队表演背后，需要耗费巨大的人力、物力和时间成本：每一架无人机的飞行路径需要经过精密的计算，每一个灯光效果都需要反复的调试，任何一个环节的疏漏都可能导致整场表演的失败。

在学习 C 语言程序设计的过程中，我深刻体会到，理论知识与实际应用之间往往存在一道难以逾越的鸿沟。课堂上学习的数据结构、算法和图形学知识，需要一个具体的、可视化的项目来将它们串联起来。无人机编队模拟恰好提供了这样一个绝佳的场景——它既包含了三维空间中的坐标计算与轨迹插值，又涉及了用户交互界面的设计与实时渲染，同时还考验着程序架构的合理性与代码的可维护性。

正是基于这样的背景，我决定开发一款轻量级的无人机编队灯光秀模拟器。不同于市面上动辄上万元的专业仿真软件，本软件以简洁实用为核心设计理念，力求用清晰的模块化代码实现尽可能完整的功能，让使用者能够在几分钟内上手，快速构建属于自己的无人机灯光秀。

### 1.2 编写目的

本软件的首要目的是为无人机编队表演提供一个**快速原型验证工具**。在实际的编队表演策划中，导演和编舞师往往需要在正式编程之前快速预览队形变换和灯光效果。传统的专业软件操作复杂、学习曲线陡峭，而本软件通过多阶段工作流（Setup → Edit → Show），让用户能够在极短的时间内完成"创建无人机 → 编排轨迹 → 回放演示"的完整流程，大大降低了原型验证的时间成本。

其次，本软件也是 C 语言与计算机图形学教学的一个**实践案例**。项目采用多文件模块化架构，将三维坐标变换、线性与样条插值、碰撞检测、快照式撤销、递归下降 JSON 解析等核心概念分散到约二十个源文件中。通过阅读和修改源码，学习者可以直观地理解实时渲染循环、轨迹插值数学、空间碰撞检测等核心概念。项目所使用的 raylib 图形库因其简洁的 API 设计和丰富的示例程序，特别适合作为从控制台编程迈向图形界面编程的第一座桥梁。

此外，本软件的设计遵循**"界面丰富、功能完整、代码精简"**的原则——在图形界面上提供充足的按钮和输入控件，给予用户完整的控制感；在底层功能上实现从轨迹编排到安全检测的完整闭环，同时保持代码模块边界清晰、避免代码膨胀。这种设计哲学使得软件既能满足当前的使用需求，又为未来的功能扩展预留了清晰的结构框架。

### 1.3 参考资料

| 书名 | 作者 | 出版社 | 出版年份 |
|------|------|--------|----------|
| 《C 高级应用程序设计》 | 王世元 | 清华大学出版社 | 2005 |
| 《C 语言程序设计教程》 | 周纯杰、何鼎新 等 | 华中科技大学出版社 | 2007 |
| 《C++ Primer Plus（第6版）》 | Stephen Prata | 人民邮电出版社 | 2012 |
| 《数据结构（C 语言版）》 | 严蔚敏、吴伟民 | 清华大学出版社 | 2007 |

### 1.4 参考软件

本项目的开发受到以下软件的启发：

- **SkyBrush 3D Drone Show Designer**：一款专业的无人机编队设计软件，其多阶段工作流（设计→仿真→部署）为本项目的模式切换架构提供了设计思路。
- **raylib 官方示例集**：raylib 图形库自带的 3D 渲染示例，尤其是 `core_3d_camera_free.c` 和 `models_billboard.c`，为本项目的 3D 场景搭建提供了直接的技术参考。
- **Blender 3D**：开源三维建模软件中的轨迹编辑器和关键帧动画系统，启发了本项目路径点轨迹编辑功能的设计。

### 1.5 编者的话

本项目从最初的一个简单想法——"能不能用 C 语言做一个看得见的无人机模拟器"——到如今一个具备完整多阶段工作流、实时 3D 渲染、轨迹样条插值和碰撞安全检测功能的图形化应用程序，中间经历了多次推倒重来和反复打磨。

最初版本将全部功能压缩在单个源文件中，随着功能的增长，单文件逐渐变得臃肿难读。为了提升可维护性，本项目经历了一次彻底的模块化重构：将 UI 渲染、无人机管理、轨迹数学、安全检测、撤销重做、JSON 存取等功能拆分到各自独立的源文件中，每个文件职责单一、边界清晰。这种"先跑通、再重构"的演进过程，让我深刻体会到软件架构并非一蹴而就，而是在持续迭代中逐步清晰起来的。

在设计过程中，我始终坚持一个原则：**让用户第一眼就知道该怎么操作**。因此，右侧面板始终保留 `1.Setup`、`2.Edit`、`3.Show` 三个醒目的模式切换按钮，每一步都有清晰的文字引导。坐标输入采用文字框直接键入的方式，坐标轴的颜色（X=红、Y=绿、Z=蓝）与输入框标签颜色一一对应，降低了空间定位的学习成本。

在轨迹这一核心能力上，本项目特别强调**一致性**：回放动画、安全碰撞检测和编辑时的轨迹预览，三者都调用同一个位置采样函数，保证"看到的路径"和"检测的路径"永远一致。这个设计决策源于对早期版本中"各模块各算各的、结果对不上"这一教训的反思。

---

## 二、任务概述

### 2.1 目标

本项目的开发目标是构建一个**轻量级、图形化、实时交互**的无人机编队灯光秀模拟器。具体而言：

1. **可视化**：在 3D 场景中实时呈现无人机的空间位置、灯光状态和运动轨迹，让用户对编队效果一目了然。
2. **可编排**：允许用户为每架无人机独立设置运动路径（路径点序列），并支持直线、缓动、样条三种平滑方式。
3. **可回放**：将编排好的轨迹以动画形式完整播放，支持播放/暂停/停止和速度调节。
4. **可检测**：在表演前运行碰撞安全预判，在播放中实时告警越界与碰撞。
5. **低门槛**：操作流程直观清晰，无需专业培训即可上手使用。
6. **可扩展**：代码结构清晰、模块化程度高，为后续功能扩展奠定基础。

### 2.2 已实现功能

目前软件已实现以下核心功能：

| 功能模块 | 具体功能 | 实现方式 |
|----------|----------|----------|
| **无人机创建** | 逐个创建无人机，键入三维坐标（X/Y/Z），从 8 种预设颜色中选择 | Setup 模式的文字输入框 + 颜色选择按钮 |
| **编队变换** | 一键把所有无人机排成圆形 / 直线 / 网格队形 | `FormCircle` / `FormLine` / `FormGrid` 函数 |
| **3D 实时预览** | 无人机以发光球体形式在正象限地面上即时显示，支持滚轮缩放、右键旋转 | raylib 3D 渲染管线 + 球坐标相机 |
| **灯光控制** | 每架无人机支持 6 种灯光模式：关/常亮/闪烁/呼吸/追逐/彩虹 | Edit 模式的灯光按钮组，相位驱动动画 |
| **路径点编辑** | 为选中无人机添加/上移/下移/复制/删除/粘贴/清空路径点 | Edit 模式的航点编辑器 |
| **平滑轨迹** | 三种轨迹模式：直线 / 缓动 / Catmull-Rom 样条曲线 | trajectory 模块统一插值 |
| **轨迹回放** | 所有无人机沿各自路径运动，播放速度 0.5x~8x 可调 | Show 模式的播放控制 + 速度滑块 |
| **安全检测** | 静态碰撞预判（时间同步模拟全程）+ 播放中实时告警 | safety 模块 + 告警弹窗 |
| **无人机管理** | 删除 / 复制 / 沿 X 中线镜像无人机，鼠标点击 3D 拾取 | 键盘快捷键 + 射线碰撞检测 |
| **撤销/重做** | 快照式 Undo/Redo（Ctrl+Z / Ctrl+Y） | undo 模块整块快照栈 |
| **文件存取** | 场景保存/加载为 JSON 文件（Ctrl+S / Ctrl+L） | 自研 JSON 解析/序列化 |
| **演出统计** | 无人机/航点数量、路径总长、预计时长、包围盒 | stats 模块 |
| **自检** | 一键运行核心算法断言测试 | test 模块 |
| **输入冲突防护** | 文字输入时自动禁用快捷键，防止误操作 | txtFocus 全局标志位机制 |
| **状态反馈** | 顶部消息栏实时显示操作结果 | Msg 消息系统 |

### 2.3 功能组织逻辑

上述功能遵循**多阶段线性工作流**：

```
Intro（欢迎） → Setup（创建） → Edit（编排） → Show（回放）
```

这种设计借鉴了视频编辑软件中的"导入素材→编辑时间线→导出播放"流程。阶段之间通过右侧面板顶部的三个大按钮自由切换，但业务逻辑上有自然的先后顺序——必须先创建无人机，才能编辑轨迹；必须先设置路径点，回放时才有运动动画。

在代码层面，程序使用一个全局枚举变量 `Mode`（取值为 `M_INTRO`、`M_SETUP`、`M_EDIT`、`M_SHOW`）来控制整个 UI 面板的分支渲染。UI 框架函数 `DrawUI()` 根据当前模式分派到对应的面板绘制函数（`DrawSetupPanel` / `DrawEditPanel` / `DrawShowPanel`），使得每种模式下的界面和逻辑相互独立、互不干扰。这种"状态机 + 分支分派"的架构，既保持了模式切换的清晰性，又让三个面板的实现分散在各自的源文件中，避免单个文件过长。

### 2.4 编写规范

- **模块划分**：每个功能域一个（或一组）`.c/.h` 文件，头文件负责对外声明，源文件负责实现；内部共享但不对外的定义放入 `*_internal.h`（如 JSON 的 `json_internal.h`）。
- **全局状态**：全局变量集中在 `common.c` 定义、`common.h` 用 `extern` 声明，采用短名（`D[]`、`N`、`S`、`M`、`spd`、`prog` 等）；UI 颜色常量采用两字母缩写（`Wh`/`Gr`/`Bl`/`Gn`/`Rd`/`Ye`/`Bg`/`Pn`/`Br`/`Bt`）。
- **函数命名**：业务函数采用描述性驼峰（`MakeDrone`、`PathLen`、`DronePosAt`、`RunSafetyCheck`、`ComputeStats`、`SaveShow` 等）；UI 控件工具函数采用三字母缩写（`Btn`、`Txt`、`Sld`、`Sep`、`Msg`、`In`）。
- **缩进风格**：采用紧凑的 K&R 风格，函数体左大括号不换行。
- **注释风格**：每个文件以块注释说明职责，关键函数以 `/* ==== 函数名 ==== */` 分隔并配中文说明，常量统一使用 `#define` 宏定义。

---

## 三、运行环境与配置

### 3.1 硬件环境

| 项目 | 最低配置 | 推荐配置 |
|------|----------|----------|
| CPU | Intel Core i3 或同等性能处理器 | Intel Core i5 及以上 |
| 内存 | 4 GB RAM | 8 GB RAM |
| 显卡 | 支持 OpenGL 3.3 的集成显卡 | 独立显卡，支持 OpenGL 4.0+ |
| 显示器 | 分辨率 1024×768 | 分辨率 1280×720 及以上 |
| 输入设备 | 鼠标 + 键盘 | 带滚轮的鼠标 |

### 3.2 软件环境

| 项目 | 说明 |
|------|------|
| **操作系统** | Windows 10 / Windows 11（64 位） |
| **编译器** | MinGW-w64 GCC |
| **图形库** | raylib 6.0（win64 预编译版本） |
| **依赖库** | OpenGL 3.3+、GLFW（随 raylib 附带） |
| **开发工具** | Visual Studio Code |
| **构建方式** | 命令行直接编译，无需 Makefile 或 CMake |

### 3.3 软件接口

本程序的软件接口关系如下：

```
┌──────────────────────────────────────────────┐
│       本项目源代码（多文件模块）              │
│   main / drone / render / ui / input          │
│   safety / trajectory / undo / json / stats   │
│   test / utils / common                       │
├──────────────────────────────────────────────┤
│  raylib.h  │  raymath.h      ← 图形渲染+数学  │
├────────────┴─────────────────────────────────┤
│  libraylibdll.a (导入库)                      │
│  raylib.dll     (动态链接库)                  │
├──────────────────────────────────────────────┤
│  OpenGL 3.3  │  GLFW         ← 底层图形 API   │
├──────────────────────────────────────────────┤
│  Windows 11  │  GPU 驱动     ← 操作系统+硬件  │
└──────────────────────────────────────────────┘
```

- **raylib**：提供窗口管理、3D 渲染（`DrawSphere`、`DrawPlane`、`DrawLine3D` 等）、输入处理（`GetMousePosition`、`IsKeyPressed`、`GetCharPressed`）和数学工具。
- **raymath**：raylib 的数学扩展，提供 `Vector3Distance`、`Vector3Normalize` 等三维向量操作函数。
- **标准 C 库**：`stdio.h`（文件读写）、`stdlib.h`（`atof` 字符串转浮点数、`malloc`/`free` 动态内存）、`string.h`（字符串与内存操作）、`math.h`（`sqrtf` 等数学函数）、`stdarg.h`（可变参数）。

### 3.4 编译命令

```bash
gcc common.c utils.c drone.c render.c ui.c ui_setup.c ui_edit.c ui_show.c \
    input.c safety.c trajectory.c undo.c \
    json.c json_parse.c json_emit.c json_store.c stats.c test.c main.c \
    -o drone_light_show.exe \
    -Iraylib/include -Lraylib/lib \
    -lraylibdll -lopengl32 -lgdi32 -lwinmm
```

链接参数说明：
- `-lraylibdll`：链接 raylib 动态导入库（raylib 的静态库由 MSVC 编译，与 MinGW 不兼容）
- `-lopengl32`：Windows OpenGL 支持
- `-lgdi32`：Windows GDI（图形设备接口）
- `-lwinmm`：Windows 多媒体 API（音频支持）

也可直接运行一键脚本 `bash build.sh`。

---

## 四、需求分析与系统设计

### 4.1 系统基本框架

本系统采用**多文件模块 + 单主循环**的经典游戏架构。所有模块共享全局状态（`common.h`），由 `main.c` 的主循环统一驱动。系统框架如下：

```
┌─────────────────────────────────────────────────────┐
│                    main() 主函数                     │
│  ┌───────────────────────────────────────────────┐  │
│  │     初始化 (InitWindow / 相机 / 球坐标参数)    │  │
│  └───────────────────┬───────────────────────────┘  │
│                      ▼                               │
│  ┌───────────────────────────────────────────────┐  │
│  │     主循环 while(!WindowShouldClose())         │  │
│  │  ┌──────────┬──────────┬──────────┬────────┐  │  │
│  │  │ Keys()   │ 相机更新  │ Pick()   │ Update │  │  │
│  │  │ 键盘处理 │ 缩放/旋转 │ 3D拾取   │ 状态更新│  │  │
│  │  └──────────┴──────────┴──────────┴────────┘  │  │
│  │  ┌──────────────────────────────────────────┐ │  │
│  │  │ Draw3D() → DrawUI() → DrawAlert() 渲染   │ │  │
│  │  └──────────────────────────────────────────┘ │  │
│  └───────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────┘
```

### 4.2 功能模块划分

程序按职责拆分为以下模块（对应各自的 `.c/.h` 文件）：

```
main.c        程序入口 + 主循环 + 相机控制
├── common     全局变量定义、常量、类型、共享声明
├── drone      无人机增删复制镜像、3D 外观、回放更新、编队变换
├── render     3D 场景绘制、鼠标 3D 拾取、鼠标地面求交
├── ui         UI 框架：欢迎界面 / 面板骨架 / 告警弹窗
│   ├── ui_setup   Setup 模式面板（创建、颜色、编队）
│   ├── ui_edit    Edit 模式面板（灯光、航点、安全、撤销、存取）
│   └── ui_show    Show 模式面板（播放、平滑模式、统计）
├── input      键盘快捷键 + 每帧更新逻辑（灯光闪烁、回放驱动）
├── safety     碰撞安全检测 + 空域判断 + 实时告警
├── trajectory 轨迹数学：缓动、Catmull-Rom 样条、路径长度、位置采样
├── undo       快照式撤销/重做
├── json       JSON 值模型 / 递归下降解析器 / 序列化器 / 场景存取
├── stats      演出统计（数量、路径长、时长、包围盒）
├── test       核心算法自检
└── utils      UI 控件（按钮/输入框/滑块/消息/HSV 转色）
```

### 4.3 数据流图

```
用户输入坐标(sx,sy,sz) → MakeDrone() → D[N].start/pos 写入
     ↓
用户输入路径点(wx,wy,wz) → [Add Waypoint] → D[S].wp[wc++] 写入
     ↓
用户点击 Play → Rst() 重置所有 → Upd() 逐帧推进 flown
     ↓
Upd() 累加 flown → DronePosAt() 采样位置 → 更新 D[i].pos
     ↓
Draw3D() 读取 D[i].pos → 渲染发光球体在 3D 场景中
```

其中 `DronePosAt()` 是贯穿全项目的核心采样函数，回放、安全检测、轨迹预览三处共用（详见 6.1）。

### 4.4 核心数据结构

```c
typedef enum { L_OFF=0, L_ON, L_BLINK, L_PULSE, L_CHASE, L_RAINBOW } Light;
typedef enum { PM_LINEAR=0, PM_EASED, PM_SPLINE } PathMode;
typedef enum { M_INTRO=0, M_SETUP, M_EDIT, M_SHOW } Mode;

typedef struct { float x, y, z; } Pt;        // 三维点
typedef struct { Pt p; } Waypoint;           // 路径点

typedef struct {
    char    name[MAX_NAME];   // 名称
    Pt      start;            // 起始位置
    Pt      pos;              // 当前位置
    float   h;                // 高度
    Waypoint wp[MAX_WP];      // 路径点数组
    int     wc;               // 路径点数量
    Light   light;            // 灯光模式
    int     color;            // 颜色编号（0~7）
    float   bt;               // 闪烁计时器
    bool    bon;              // 闪烁亮/灭状态
    int     ci;               // 当前路径点索引
    bool    fin;              // 是否飞完
    bool    act;              // 是否激活
    bool    sel;              // 是否选中
    float   flown;            // 累计已飞行距离（平滑回放用）
    float   ph;               // 灯光效果相位
    float   espeed;           // 灯光效果速度倍率
} Drone;
```

`Drone` 结构体是整个程序的数据核心，所有无人机存放在全局数组 `D[MAX_DRONES]` 中（`MAX_DRONES = 200`），`N` 记录数量，`S` 记录选中索引。将 `Pt` 定义为独立的三维点结构体（而非直接使用 raylib 的 `Vector3`），是为了在数据层与图形库之间保持一定独立性。

---

## 五、界面设计与功能介绍

### 5.1 整体布局

程序窗口分为三个区域：

| 区域 | 位置 | 内容 |
|------|------|------|
| **3D 视图区** | 左侧主区域 | 实时 3D 渲染场景，仅显示正象限（X≥0, Z≥0），包含地面、网格线、坐标轴、空域边界框、无人机球体、路径点标记 |
| **控制面板** | 右侧 240px 宽 | 模式切换按钮 + 当前模式对应的操作控件 |
| **帮助栏** | 左下角 | 半透明背景的快捷键速查表 + 当前模式和无人机数量 |

### 5.2 3D 场景设计

3D 场景是本项目的核心视觉输出。本软件采用**1/4 象限设计**——仅显示 X≥0、Z≥0 的正半平面，将原点置于场景远端角落处。这一设计决策基于以下考量：

- **空间直观性**：用户只需输入正数坐标（X、Y、Z 均≥0），无人机便会出现在对应的网格位置上。数字越大，离原点越远，方向感非常清晰。
- **边界可视化**：红线（X 轴）和蓝线（Z 轴）自然形成了地面的两条边界线，绿线（Y 轴）从原点竖直向上延伸。三条轴线形成类似于房间角落的空间参考。
- **减轻认知负担**：去掉负半平面的网格线后，场景更加清爽。

场景地面为 40×40 单位的半透明灰色平面，上面绘制 20×20 的淡色参考网格，并以淡黄色线框标出允许飞行的空域范围（X/Z 0~40 米、Y 0.5~30 米）。原点处有一个红色小球标记。相机采用**球坐标**控制：初始位于远角高处俯视，用户可通过滚轮缩放（5~60 单位）、按住鼠标右键拖拽环绕旋转、按 F 键聚焦到选中无人机。

### 5.3 Setup 模式 — 无人机创建界面

Setup 模式是用户创建无人机的界面。标题下方依次排列：

**坐标输入区**：三个独立文字输入框，标记为红色的 X、绿色的 Y、蓝色的 Z，与 3D 场景坐标轴颜色对应。Y 轴初始值为 0（贴近地面），创建时自动钳制在 0.5~30 米。

**颜色选择区**：8 种预设颜色（红/绿/蓝/黄/青/品红/橙/白），两行四列排布，当前选中的颜色显示白色加粗边框。

**创建按钮与列表**：点击 `+ Create Drone`（或按 A 键）生成无人机，同时出现在已创建列表中。列表每项显示编号、名称、坐标和颜色小色块；点击列表中的某项可直接选中并跳转到 Edit 模式。

**编队变换区**：`Circle` / `Line` / `Grid` 三个按钮，一键把所有无人机分别排成圆形、直线或网格队形（只改起始位置，保留各自航点）。

**跳转按钮**：`-> Continue to Edit` 进入编辑模式。

### 5.4 Edit 模式 — 轨迹编排界面

Edit 模式是轨迹编排的核心界面，其下分为多个功能区块：

**灯光控制区**：6 个按钮 `OFF` / `ON` / `Blink` / `Pulse` / `Chase` / `Rainbow` 控制选中无人机的灯光模式，当前激活的模式以对应语义颜色高亮。

**路径点添加区**：三个坐标输入框 + `+ Add Waypoint` 按钮，将输入的坐标作为一个路径点追加到选中无人机轨迹末尾（越界则弹窗提示）。

**路径点列表**：显示已添加的路径点，每条右侧有 `^`（上移）、`v`（下移）、`C`（复制到剪贴板）、`X`（删除）按钮；顶部还有 `Paste`（粘贴剪贴板航点）和 `Clear All`（清空）。3D 视图中路径点以黄色小球标记、黄色虚线连接，并叠加蓝色平滑曲线预览实际飞行轨迹。

**辅助操作**：`Delete Drone`（删除）、`Duplicate`（复制整机并偏移）、`Mirror X`（沿 X 中线镜像路径）。

**安全检测区**：`Safety Check` 按钮运行碰撞预判，结果显示碰撞风险数量及详情。

**工具条**：`Undo` / `Redo` / `Save` / `Load` 按钮，以及 `Self-Test` 自检按钮。

**输入冲突防护**：当用户在文字输入框中键入坐标时，程序通过 `txtFocus` 全局标志自动禁用 `1`/`2`/`3` 灯光快捷键和 `A` 创建键等，防止输入数字时误触功能。

### 5.5 Show 模式 — 回放界面

Show 模式提供轨迹动画的完整播放控制：

**速度调节**：可拖动的速度滑块（0.5x~8x），是程序中唯一保留的滑块控件。

**轨迹平滑模式**：`Linear` / `Eased` / `Spline` 三个按钮切换平滑方式。

**播放控制**：`Play`（绿）、`Pause`（黄）、`Stop`（红）三个按钮，与通用播放器布局一致；进度条以黄色填充条实时显示回放百分比。

**演出统计**：显示无人机数量、航点总数、路径总长、最长/平均路径、预计时长、已用时间，以及整场演出占用的包围盒范围。

**快捷键**：空格键播放/暂停，Esc 停止。

---

## 六、核心算法说明

### 6.1 轨迹插值（trajectory 模块）

轨迹模块是整个项目的数学核心，提供三类能力：

**缓动函数**：`EaseLinear`、`EaseInQuad`、`EaseOutQuad`、`EaseInOutCubic`、`SmoothStep`，将 0~1 的线性进度重新映射为平滑进度（均满足 `Ease(0)=0`、`Ease(1)=1`），使无人机在每段起点加速、终点减速。

**Catmull-Rom 样条**：用相邻四个控制点定义一段曲线，使相邻段方向连续、不折角，实现穿过航点的平滑曲线。

**路径长度与位置采样**：`PathLen()` 逐段累加出完整路径长度；`DronePosAt(d, s, mode)` 是核心采样函数——给定"已飞行距离 s"和"平滑模式 mode"，返回无人机在该处的三维位置。其按段推进、在段内按 mode 插值：

- `PM_LINEAR` → 直线匀速
- `PM_EASED` → 直线 + 缓动（加速→减速）
- `PM_SPLINE` → Catmull-Rom 曲线

**关键设计：三处共用一个采样函数**。回放动画（`Upd`）、安全检测（`RunSafetyCheck`）和编辑时的轨迹预览（`DD`）都调用 `DronePosAt`，因此"回放看到的路径"与"安全检测计算的路径"永远一致，从根本上避免了各模块插值不一致导致的错误。

### 6.2 碰撞安全检测（safety 模块）

安全检测分为两类：

**静态碰撞预判**（`RunSafetyCheck`）：以"飞行距离 s"统一表示时间——所有无人机以相同线速度同时起飞，走过相同距离即同一时刻。让 s 从 0 增长到最长路径长度（步长 `SIM_STEP = 0.25` 米，上限 `MAX_STEPS = 4000`），每步用 `DronePosAt` 采样两两无人机的位置并计算距离，记录全程最小距离；最小距离小于安全间距 `SAFE_DIST = 0.8` 米即记为碰撞风险。

**实时告警**（`LiveCheck`）：播放中每帧检查所有无人机当前实际位置，发现越界（空域 X/Z 0~40、Y 0.5~30 米之外）或两两距离过近时，弹出告警弹窗并暂停回放。

此外，`InAirspace()` 用于创建无人机、添加航点时即时校验坐标是否越界。

### 6.3 撤销/重做（undo 模块）

采用**快照**方案：每次变更前把整个无人机数组 `D[]` 连同数量 `N`、选中索引 `S` 整块 `memcpy` 存为一份快照，撤销时恢复上一份快照。撤销栈与重做栈深度各为 `UNDO_DEPTH = 32`。

这种方案代码极简（约 100 行），且天然适用于所有类型的变更——增删无人机、加删航点、移动、改灯光、编队变换都无需为每种操作单独写命令。代价是内存（一份快照 = 200 个 `Drone` 结构体，两套栈合计约数 MB），对桌面程序完全可接受。

### 6.4 JSON 存取（json 模块）

项目自带一个**极简 JSON 库**，而非引入第三方依赖：

- **值模型**（`json.c`）：`JVal` 用不透明类型对外隐藏内部结构（定义在 `json_internal.h`），支持 null/bool/num/str/arr/obj 六种类型，堆上动态分配。
- **解析器**（`json_parse.c`）：递归下降解析，每个语法结构对应一个函数（`parseObject`/`parseArray`/`parseValue`），支持字符串转义。
- **序列化器**（`json_emit.c`）：用可增长的字符串缓冲拼出带缩进的可读 JSON 文本。
- **场景存取**（`json_store.c`）：`SaveShow` 把 `D[]/N/pathMode` 写入文件，`LoadShow` 从文件还原，两者构成完整的场景持久化。

---

## 七、关键函数说明

| 函数 | 所在文件 | 说明 |
|------|----------|------|
| `MakeDrone` / `DelDrone` | drone.c | 创建 / 删除无人机（前移覆盖） |
| `DuplicateDrone` / `MirrorPath` | drone.c | 复制整机 / 沿 X 中线镜像路径 |
| `FormCircle` / `FormLine` / `FormGrid` | drone.c | 圆形 / 直线 / 网格编队变换 |
| `RC` / `DD` | drone.c | 计算无人机渲染颜色 / 绘制单架无人机 |
| `Rst` / `Upd` | drone.c | 重置回放 / 逐帧推进回放动画 |
| `Draw3D` / `Pick` / `MouseGround` | render.c | 绘制 3D 场景 / 射线拾取 / 鼠标地面求交 |
| `Keys` / `Update` | input.c | 键盘快捷键 / 每帧更新逻辑 |
| `DrawUI` / `DrawStartScreen` / `DrawAlert` | ui.c | UI 框架（面板骨架 / 欢迎界面 / 告警弹窗） |
| `DrawSetupPanel` / `DrawEditPanel` / `DrawShowPanel` | ui_setup.c / ui_edit.c / ui_show.c | 三种模式的面板内容 |
| `Ease` / `CatmullRom` / `PathLen` / `DronePosAt` | trajectory.c | 缓动 / 样条 / 路径长度 / 位置采样 |
| `RunSafetyCheck` / `SafetyWarn` / `InAirspace` / `LiveCheck` | safety.c | 碰撞预判 / 风险标记 / 空域判断 / 实时告警 |
| `UndoPush` / `Undo` / `Redo` | undo.c | 记录快照 / 撤销 / 重做 |
| `JsonParse` / `JsonEmit` / `SaveShow` / `LoadShow` | json*.c | JSON 解析 / 序列化 / 场景存取 |
| `ComputeStats` | stats.c | 计算演出统计信息 |
| `RunSelfTest` | test.c | 运行核心算法自检 |
| `Msg` / `Btn` / `Txt` / `Sld` / `Hsv2Rgb` | utils.c | 消息 / 按钮 / 输入框 / 滑块 / HSV 转色 |

---

## 附：项目文件清单

| 文件 | 说明 |
|------|------|
| `main.c` | 程序入口 + 主循环 + 相机控制 |
| `common.c` / `common.h` | 全局变量定义 / 公共声明 |
| `drone.c` / `drone.h` | 无人机操作 + 回放 + 编队变换 |
| `render.c` / `render.h` | 3D 渲染 + 拾取 |
| `ui.c` / `ui.h` | UI 框架 |
| `ui_setup.c` / `ui_edit.c` / `ui_show.c` | 三种模式面板 |
| `input.c` / `input.h` | 键盘输入 + 更新逻辑 |
| `safety.c` / `safety.h` | 碰撞安全检测 |
| `trajectory.c` / `trajectory.h` | 轨迹数学 |
| `undo.c` / `undo.h` | 快照式撤销/重做 |
| `json.c` / `json.h` / `json_parse.c` / `json_emit.c` / `json_store.c` / `json_internal.h` | JSON 库与场景存取 |
| `stats.c` / `stats.h` | 演出统计 |
| `test.c` / `test.h` | 算法自检 |
| `utils.c` / `utils.h` | UI 控件工具 |
| `raylib/` + `raylib.dll` | raylib 6.0 头文件、导入库、动态库 |
| `build.sh` | 一键编译脚本 |
