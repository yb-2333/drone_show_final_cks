/******************************************************************************
 *  input.h  -  键盘输入 + 更新逻辑模块声明
 *
 *  处理所有键盘快捷键和每帧数据更新（闪烁、消息计时等）。
 ******************************************************************************/
#ifndef INPUT_H
#define INPUT_H

#include "common.h"

/* 处理键盘快捷键（Tab切换、方向键移动、数字键灯光等） */
void Keys(void);

/* 每帧更新逻辑：闪烁计时、消息计时、回放动画 */
void Update(float dt);

#endif  /* INPUT_H */
