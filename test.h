/******************************************************************************
 *  test.h  -  自检模块声明
 *
 *  提供一组不依赖图形界面的"自检"（self-test）：对轨迹数学、缓动、
 *  样条、JSON 等核心算法跑断言，检查结果是否符合预期。
 *  方便初学者在改动算法后，一键验证有没有改坏。
 ******************************************************************************/
#ifndef TEST_H
#define TEST_H

/*
 * RunSelfTest() - 运行全部自检，返回失败的断言数量（0 = 全部通过）。
 * 结果会通过 Msg() 打印到屏幕。
 */
int RunSelfTest(void);

#endif  /* TEST_H */
